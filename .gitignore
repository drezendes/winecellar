# Environment / secrets
.env
# SSH keys for provisioning — private stays in the 1Password agent; box.pub is
# regenerable via `op read`. Nothing key-shaped gets committed.
keys/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Django
db.sqlite3
media/
staticfiles/
logs/

# Shared handoff / "working with the owner" notes — this repo is public, so the
# cross-project shared-memory doc lives in the private blog repo instead
# (blog/docs/shared_memory.md). Kept out of history here on purpose.
docs/blog_agent_handoff.md

# Tooling
.pytest_cache/

# Local output (analysis dumps etc.)
output_files/
