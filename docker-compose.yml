services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////app/data/db.sqlite3
    volumes:
      - ./data:/app/data
      - ./media:/app/media
      - ./logs:/app/logs
